# ─────────────────────────────────────────────────────────
#  SecureVault · Python Dependencies
#  Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────
flask>=3.0.0

# ── Rate Limiting ─────────────────────────────────────────
flask-limiter>=3.5.0

# ── AES-256-GCM Encryption (Military Grade) ──────────────
cryptography>=42.0.0

# ── Supabase Authentication ───────────────────────────────
supabase>=2.3.0

# ── Image Processing ──────────────────────────────────────
opencv-python-headless>=4.9.0.80
numpy>=1.26.0

# ── Environment Variables ─────────────────────────────────
python-dotenv>=1.0.0

# ── HTTPS support (dev/local self-signed cert) ───────────
pyopenssl>=24.0.0

# ── Redis (optional — for distributed rate limiting) ──────
# Uncomment if using Redis for RATELIMIT_STORAGE_URL
# redis>=5.0.0

# ── WSGI Server (production deployment) ───────────────────
# Use Gunicorn instead of Flask dev server in production:
# gunicorn>=21.2.0
